@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager

@model string[]

@{
    ViewData["Title"] = "Home ";
}

@if (!SignInManager.IsSignedIn(User))
{
    <partial name="_AnonymousUserPartial" />
}
else
{
    <partial name="_LoggedUserPartial" />
}